# Task 1 — architecture design judge
#
# The judge calls the model through agent/llm.py, so the provider SDK comes
# from the repository's own requirements. These are the extras needed to read
# submissions that are not plain text.

python-dotenv>=1.0
python-docx>=1.1      # .docx submissions
pypdf>=4.0            # .pdf submissions
