# Workshop participants: this is the only file you need.
#
#     pip install -r requirements.txt
#
# It installs the hosted-Claude path (Bedrock). It does NOT install torch —
# nothing in the workshop runs a model on your laptop.

python-dotenv>=1.0
pandas>=2.0

# --- Bedrock / Anthropic ----------------------------------------------------
# 0.88 is the floor, not a suggestion: Bedrock API key (bearer token) auth
# was added in 0.88.0. On an older version AWS_BEARER_TOKEN_BEDROCK is
# silently ignored and you get a credentials error that names nothing useful.
anthropic>=0.88

# Only needed if you authenticate with IAM keys (AWS_ACCESS_KEY_ID /
# AWS_SECRET_ACCESS_KEY) instead of a Bedrock API key. Harmless otherwise.
boto3>=1.35

# --- Task 1: reading submissions --------------------------------------------
python-docx>=1.1
pypdf>=4.0
